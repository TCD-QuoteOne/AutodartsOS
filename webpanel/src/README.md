# Web Panel

The first implementation is intentionally a single Python script installed as `/usr/local/bin/autodarts-webpanel`.

Future work:

- setup wizard
- camera preview
- update controls
- log viewer
- backup and restore

