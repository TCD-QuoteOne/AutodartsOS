if [ -z "${DISPLAY:-}" ] && [ "$(tty)" = "/dev/tty1" ]; then
  clear
  echo "Autodarts Pi OS verwaltet den Bildschirm ueber systemd."
  echo
  echo "Status:"
  systemctl --no-pager status autodarts-kiosk.service || true
  echo
  echo "Setup-Adressen:"
  echo "  http://10.42.0.1"
  echo "  http://auto.setup.go"
  echo "  http://autodarts-pi.local"
fi
